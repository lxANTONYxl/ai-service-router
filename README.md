# AI Service Router

API Gateway que distribuye requests entre múltiples servicios de IA (Groq, Cerebras).

## Configuración

1. Clona el repositorio
2. Configura tus API keys en `appsettings.Development.json`
3. Ejecuta: `dotnet run`

## Endpoint

POST `/chat`